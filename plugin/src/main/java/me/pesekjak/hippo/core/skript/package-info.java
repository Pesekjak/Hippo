/**
 * Contains wrappers of ASM core elements used to inject the
 * method and field writers.
 * <p>
 * Stores all currently existing custom classes, triggers of their executables, and
 * expression of their fields.
 * <p>
 * Serves as a bridge to easily invoke the user Skript code from compiled custom classes.
 */
package me.pesekjak.hippo.core.skript;
